[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate deploy --config prisma/prisma.config.ts && npm run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
